import tkinter as tk

root = tk.Tk()

label = tk.Label(root, text="Hello Python")
label = tk.Label(
    root,
    text="Hello Python",
    font=("Arial", 20),
    padx=20,
    pady=20
)

label.pack()

button = tk.Button(
    root,
    text="Click Me",
    command="hello bhga jaa"
)

button.pack()



root.mainloop()